//! Database utilities (placeholder module for queries and helpers).

// Database queries are currently inline via sqlx::query_as! macros in the API handlers.
// As the project grows, shared query functions will be extracted here.
